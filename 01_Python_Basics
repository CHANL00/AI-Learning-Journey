一、列表
1.1列表的增删改查
增加：xxx.append('x')将x元素添加到xxx列表末尾。
插入：xxx.insert(0,'x')在索引0处添加x元素。
删除：del xxx[0]知道要删除的元素在列表的位置，可以用del语句。
     y=xxx.pop() pop()可以删除列表末尾，并且可以接着使用该元素。
     xxx.remove('y') remove()可以删掉列表中对应值的元素。
修改：xxx[0] = 'y' 
1.2列表的排序
xxx.sort()永久性排序，按字母排序；sorted(xxx)临时排序；xxx.sort(reverse=True)
反转列表位置xxx.reverse()
列表排序 len(xxx)

二、列表的操作
2.1遍历列表
2.2数值列表的创建
2.3列表切片
2.4元组
三、if语句
3.1条件测试
3.2if语句
四、字典
4.1字典的使用
4.2遍历字典
4.3嵌套
五、用户输入和while循环
六、函数
