一、数据容器
1.1列表[]
1.1.1列表的增删改查
增加：1.xxx.append('x')将x元素添加到xxx列表末尾。
     2.xxx.extend(其它数据容器),将其他数据容器的内容取出，依次追加到列表尾部。
插入：xxx.insert(0,'x')在索引0处添加x元素。
删除：del xxx[0]知道要删除的元素在列表的位置，可以用del语句。
     y=xxx.pop()，pop()可以删除列表末尾，并且可以取出该元素。xxx.pop(下标)也可以删除下标位置元素并且取出。
     xxx.remove('y')，remove()可以删掉某元素在列表中的第一个匹配项。
清空列表：xxx.clear()。
修改：xxx[0] = 'y' 可直接修改。
查询（方法）：xxx.index(y) 查找指定元素在列表的下标。
1.1.2列表的排序
永久性排序：xxx.sort()，按字母排序，xxx.sort(reverse=True)，反向排序；
临时性排序：sorted(xxx)，按字母排序，sorted(xxx,reverse=True)，反向排序；
反转列表位置：xxx.reverse()，没有排序直接按照原位置反转。 
列表元素计数：len(xxx)。
统计某元素在列表内的数量：xxx.count(元素)。

1.1.3列表的操作
(1)遍历列表
for i in xxx:
(2)数值列表的创建
xxx = list(range(a,b,c)),a代表起始位置，b代表末尾-1，c为步长（省略的话，步长为1）。
有专门用于处理数字列表的Python函数如：min(),max(),sum()。
列表解析：xxx = [value**2 for value in range(1,11)]     相当于     squares = [] 
                                                                  for value in range(1,11): 
                                                                       square = value**2                                                                  
                                                                       squares.append(square)

1.3元组() tuple     和列表基本相同，唯一不同在于不可修改。
1.3.1定义元组
t1=(1,'a',True),t2=(),t3=tuple()。定义单个元素的元组需要加‘，’：t4=('a',)。
1.3.2元组的相关操作
index(元素)查找某个数据，返回下标。
count(元素)统计某个数据在当前元组出现的次数。
len(元组)统计元组内的元素个数。
"""元组内的元素不可修改，但嵌套下的元组内的列表内的元素可以修改"""

1.4字符串str""     字符串是不可变的。
1.4.1字符串的操作
字符串.index(字符串) ：查找给定字符的第一个匹配项的下标。
字符串.replace(字符串1，字符串2) ：将字符串内的匹配的全部字符串1，替换为字符串2，得到新的字符串。
字符串.split(字符串1) :按照给定字符串1，对字符串进行分离，得到一个新的列表。
字符串.strip() :移除首尾的空格和换行符 ;字符串.strip(字符串1) :移除首尾指定字符串1 。
字符串.count(字符串1) :统计字符串内字符串1出现的次数。
len(字符串) ：统计字符串的字符个数。

1.5序列的切片     列表、元组、字符串都属于序列
players[a:b:c]，abc为数值，a代表起始位置（省略的话，从起始位置开始），b代表末尾-1（省略的话，到末尾位置结束）,c代表步长（省略为1，可以为负数）。
复制列表：friend_foods = my_foods[:]

1.6集合{} set()     无序存储，去重不允许重复的数据存在，不支持下标索引。
#定义空集合:set()
集合.add(元素) 添加新元素到集合。
集合.remove(元素) 移除元素。
集合.pop() 随机取出元素。
集合.clear() 清空集合。
set3=set1.difference(set2)     取出set1和set2的差集，set1中set2没有的元素。
set1.difference_update(set2)   消除set1中和set2相同的元素。
set3=set1.unior(set2)          两个集合合并。
统计集合中的元素数量 ：len(set1)。

1.7字典{} dict     Key:Value     Key不可以重复     不可以下标索引
1.7.1字典的使用
#定义空字典:dict1={}     dict1=dict()
#定义字典变量：dict1={Key1:Value,Key2:Value,Key3:Value}
字典可以通过Key获取Value ：dict1[Key1]
1.7.2字典常用操作
dict1["xxx"]=元素     如"xxx"不存在于dict1中就是新增元素；存在就是更新元素。
删除元素 ：x=字典.pop(Key)     获取指定Key的Value，同时字典被修改，指定Key的数据被删除。
清空字典 ：字典.clear()。
获取全部的Key ：字典.keys()。
遍历字典 ：(1)for key in dict1.keys():
           (2)for key in dict1:
统计字典中的元素数量 ：len(dict1)

1.7.3嵌套     字典的Key和Value可以是任意数据类型(Key不可以为字典)
#从嵌套字典中获取数据
#获取xxx的语文信息 ： score_dict={"xxx":{"语文":99, "数学":88, "英语":77}}     score=score_dict["xxx"]["语文"]

1.8总结
支持下标索引：列表、元组、字符串；不支持下标索引：集合、字典。
支持重复元素：列表、元组、字符串；不支持重复元素：集合、字典。
支持可以修改：列表、集合、字典；不支持修改：元组、字符串。
应用场景:列表：一批数据，可修改、可重复的存储场景。
         元组：一批数据，不可以修改、可重复的存储场景。
         字符串：一串字符串的存储场景。
         集合：一批数据，去重存储场景。
         字典：一批数据，可用Key检索Value的存储场景。

二、for循环和while循环
2.1for循环
for 临时变量 in 待处理数据集(序列):
     循环满足条件时执行的代码
2.2while循环
while 条件判断:
     条件满足时，做的事情1。
三、输入输出和if语句
3.1输入输出语句
输入语句：input()。
输出语句：print(f"{变量}xxxxxxx")     print("xxx"+变量+"xxx")
3.2if语句
if 判断：
     条件满足时，做的事情1
elif 判断：
     条件满足时，做的事情2
else：
     所有if和elif条件都不满足时，做的事情3
四、函数
4.1接收多个返回值
def test_return():               """按照返回值的顺序，写对应顺序与的多个变量接受即可"""
     return 1,2                  """变量之间用逗号隔开，支持不同类型的数据return"""

x,y=test_return()
4.2函数参数种类
(1).位置参数：调用函数是根据函数定义的参数位置来传递参数。
(2).关键字参数：函数调用时通过“键=值”形式传递参数。     fun(name="小明",age=20,gender="男")     可以不按照固定顺序，也可以和位置参数混用，但位置参数必须在前。
(3)缺省参数（默认参数）：用于定义函数，为参数提供默认值，调用参数时可不传该默认参数的值。     def fun(name,age,gender='男')     当调用函数时没有传递参数，就会使用默认是用缺省参数对应的值。设置默认值必须放在最后。
(4)不定长参数（可变参数）：用于不确定调用的时候会传递多少个参数（不传参也可以）的场景。     类型有位置传递 关键字传递。     
def fun(*args)     """传进的所有参数都会被args变量收集，它会根据传进参数的位置合并为一个元组，args是元组类型，这就是位置传递。
def fun(**kwargs)     """参数是“键=值”形式的情况下，所有的“键=值”都会被kwargs接受，同时会根据“键=值”组成字典。"""







